extends ../layout
block content 
    h2 Your code is: #{user.code} 
    p Share your code with a student so they can add you as an educator
    p Or add a student by entering the code they provide: 
        form(action="/addstudent", method="POST")
            input(type="text", id="code", name="code") 
            button.submit(type="submit") Submit
    h2 Your students:
    table
        tr 
            th Username 
            th Email address 
            th Results
        each student in students
            tr 
                td #{ student.name } 
                td #{ student.email }
                td 
                    a(href="#") View results 

